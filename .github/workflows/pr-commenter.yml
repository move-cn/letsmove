on:
  pull_request:

permissions: write-all

jobs:
  pr:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: mshick/add-pr-comment@v2
        if: always()
        with:
          message: |
            **hello!**
          message-failure: |
            Uh oh!