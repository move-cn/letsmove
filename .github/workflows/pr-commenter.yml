on:
  pull_request_target:

jobs:
  pr:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: mshick/add-pr-comment@v2
        if: always()
        with:
          message: |
        
            **必须加入社区发出PR才会被合并**
            - QQ群: 79489587
            - TG群: [Move中文](https://t.me/move_cn)
            **请仔细阅读相关任务需求**
          message-failure: |
            Uh oh!